---
gitlab_runner_registration_token: "{{ (gitlab_runner_secrets.stdout | from_json)[\"gitlab-runner-token\"] }}"
gitlab_runner_private_key: "{{ (gitlab_runner_secrets.stdout | from_json)[\"gitlab_runner_private_key\"] }}"
